<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="0"
        android:key="subject_style"
        android:entries="@array/subject_formats"
        android:entryValues="@array/subject_formats_index"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/subject_format_title" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="EEE, MMM dd hh:mm aa z"
        android:entries="@array/date_formats"
        android:entryValues="@array/date_format_strings"
        android:key="date_format"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/date_format_title" />

    <!--android:capitalize="words"-->
    <!--android:inputType="textCapWords"-->

</PreferenceScreen>